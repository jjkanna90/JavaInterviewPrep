package org.prep.lists;

import org.prep.utility.SListNode;

public class SumListsPartialSum {

    int carry;
    SListNode sum;

    public SumListsPartialSum(int carry, SListNode sum){
        this.carry = carry;
        this.sum = sum;
    }
}
